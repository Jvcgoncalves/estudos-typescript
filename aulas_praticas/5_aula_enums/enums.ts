enum Planets{ 
  MERCURIO,
  VENUS,
  TERRA,
  MARTE
}

enum Roles{
  ADMIN = "admin",
  USER = "user"
}

// enum é um conjunto de constanstes que podemos atribuir nomes à essas constantes, sendo o valor padrao delas um número ou podemos atribuir string ou até numeros começando de um outro index